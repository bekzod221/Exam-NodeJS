<header class="navbar">
    <div class="nav-wrapper">
        <div class="nav-left">
            <a href="/" class="brand">
                <span class="brand-icon">🚗</span>
                AutoLux
            </a>
            <div class="breadcrumbs">
                <span class="breadcrumb-item">Home</span>
                <% if (typeof breadcrumbs !== 'undefined' && breadcrumbs.length > 0) { %>
                    <% breadcrumbs.forEach(function(breadcrumb, index) { %>
                        <span class="breadcrumb-separator">></span>
                        <span class="breadcrumb-item"><%= breadcrumb %></span>
                    <% }); %>
                <% } %>
            </div>
        </div>
        <nav>
            <a href="/">Home</a>
            <a href="/models">Models</a>
            <a href="/categories">Categories</a>
            <a href="/profile" class="user-only">Profile</a>
            <a href="/bookmarks" class="user-only">Favorites</a>
            <a href="/cart" class="cart-link user-only">
                <span class="cart-icon">🛒</span>
                <span class="cart-count" id="cartCount">0</span>
            </a>
            <button id="logoutBtn" class="link user-only">Sign Out</button>
            <a href="/login" class="guest-only">Sign In</a>
            <a href="/register" class="guest-only">Sign Up</a>
            <a href="/admin-login" class="admin-btn">
                <i class="admin-icon">👤</i>
                Admin Panel
            </a>
        </nav>
    </div>
</header>

<script>
document.getElementById('logoutBtn')?.addEventListener('click', async () => {
    try {
        const response = await fetch('/api/auth/logout', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            }
        });
        
        if (response.ok) {
            window.location.href = '/';
        }
    } catch (error) {
        console.error('Logout failed:', error);
    }
});
</script>
